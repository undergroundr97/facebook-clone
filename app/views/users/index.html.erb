<p style="color: green"><%= notice %></p>

<% content_for :title, "Users" %>

<h1>Users</h1>

<div id="users">
  <% @users.each do |user| %>
    <% next if user == current_user%>
    <% next if user.received_invites.ids.include?(current_user.id)%>
    <%= render user %>

      <%# <%= link_to "Show this user", user %>
 
  <% end %>
</div>

<%= link_to "New user", new_user_path %>
